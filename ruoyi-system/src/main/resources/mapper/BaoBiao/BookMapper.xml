<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.system.mapper.BaoBiao.BookMapper">

    <resultMap id="BookMap" type="com.ruoyi.system.domain.BaoBiao.Book">
        <id     property="id"         column="id"/>
        <result property="title"      column="title"/>
        <result property="author"     column="author"/>
        <result property="price"      column="price"/>
        <result property="status"     column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, author, price, status, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BookMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_book
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BookMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_book
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND author LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="com.ruoyi.system.domain.BaoBiao.Book" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_book (title, author, price, status)
        VALUES (#{title}, #{author}, #{price}, #{status})
    </insert>

    <update id="update" parameterType="com.ruoyi.system.domain.BaoBiao.Book">
        UPDATE t_book
        SET title=#{title},
            author=#{author},
            price=#{price},
            status=#{status}
        WHERE id=#{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_book WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM t_book WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
