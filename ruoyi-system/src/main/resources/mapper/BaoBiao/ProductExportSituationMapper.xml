<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.system.mapper.BaoBiao.ProductExportSituationMapper">

    <resultMap id="ProductExportSituationMap" type="com.ruoyi.system.domain.BaoBiao.ProductExportSituation">
        <id     property="id"                 column="id"/>
        <result property="exportDate"         column="export_date"/>
        <result property="unitCode"           column="unit_code"/>
        <result property="unitName"           column="unit_name"/>

        <result property="totalCars"          column="total_cars"/>
        <result property="cleanCoalCars"      column="clean_coal_cars"/>
        <result property="lumpCoalCars"       column="lump_coal_cars"/>
        <result property="slackCoalCars"      column="slack_coal_cars"/>
        <result property="mixedCoalCars"      column="mixed_coal_cars"/>

        <result property="totalTonnage"       column="total_tonnage"/>
        <result property="cleanCoalTonnage"   column="clean_coal_tonnage"/>
        <result property="lumpCoalTonnage"    column="lump_coal_tonnage"/>
        <result property="slackCoalTonnage"   column="slack_coal_tonnage"/>
        <result property="mixedCoalTonnage"   column="mixed_coal_tonnage"/>

        <result property="createTime"         column="create_time"/>
        <result property="updateTime"         column="update_time"/>
        <result property="userId"             column="user_id"/>
        <result property="isDeleted"          column="is_deleted"/>
        <result property="mineCategory"       column="mine_category"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, export_date, unit_code, unit_name,
        total_cars, clean_coal_cars, lump_coal_cars, slack_coal_cars, mixed_coal_cars,
        total_tonnage, clean_coal_tonnage, lump_coal_tonnage, slack_coal_tonnage, mixed_coal_tonnage,
        create_time, update_time, user_id, is_deleted, mine_category
    </sql>

    <select id="selectById" resultMap="ProductExportSituationMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_export_situation
        WHERE id = #{id}
        AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="ProductExportSituationMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_export_situation
        <where>
            is_deleted = 0
            <if test="unitCode != null and unitCode != ''">
                AND unit_code = #{unitCode}
            </if>
            <if test="exportDate != null">
                AND export_date = #{exportDate}
            </if>
        </where>
        ORDER BY export_date DESC, id DESC
    </select>

    <insert id="insert" parameterType="com.ruoyi.system.domain.BaoBiao.ProductExportSituation"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_export_situation (
            export_date, unit_code, unit_name,
            total_cars, clean_coal_cars, lump_coal_cars, slack_coal_cars, mixed_coal_cars,
            total_tonnage, clean_coal_tonnage, lump_coal_tonnage, slack_coal_tonnage, mixed_coal_tonnage,
            user_id, is_deleted, mine_category, create_time, update_time
        ) VALUES (
                     #{exportDate}, #{unitCode}, #{unitName},
                     #{totalCars}, #{cleanCoalCars}, #{lumpCoalCars}, #{slackCoalCars}, #{mixedCoalCars},
                     #{totalTonnage}, #{cleanCoalTonnage}, #{lumpCoalTonnage}, #{slackCoalTonnage}, #{mixedCoalTonnage},
                     #{userId}, 0, #{mineCategory}, NOW(), NOW()
                 )
    </insert>

    <update id="update" parameterType="com.ruoyi.system.domain.BaoBiao.ProductExportSituation">
        UPDATE product_export_situation
        SET export_date = #{exportDate},
            unit_code = #{unitCode},
            unit_name = #{unitName},
            total_cars = #{totalCars},
            clean_coal_cars = #{cleanCoalCars},
            lump_coal_cars = #{lumpCoalCars},
            slack_coal_cars = #{slackCoalCars},
            mixed_coal_cars = #{mixedCoalCars},
            total_tonnage = #{totalTonnage},
            clean_coal_tonnage = #{cleanCoalTonnage},
            lump_coal_tonnage = #{lumpCoalTonnage},
            slack_coal_tonnage = #{slackCoalTonnage},
            mixed_coal_tonnage = #{mixedCoalTonnage},
            mine_category = #{mineCategory},
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <delete id="deleteById">
        UPDATE product_export_situation
        SET is_deleted = 1, update_time = NOW()
        WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        UPDATE product_export_situation
        SET is_deleted = 1, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>


    <select id="selectMonthList" resultMap="ProductExportSituationMap">
        SELECT unit_code,sum(total_tonnage) AS total_tonnage
        FROM product_export_situation
        where
            is_deleted = 0
            AND YEAR(export_date)=YEAR(#{exportDate}) AND MONTH(export_date)=MONTH(#{exportDate})  AND unit_code = #{unitCode}
        group by unit_code
    </select>
</mapper>
